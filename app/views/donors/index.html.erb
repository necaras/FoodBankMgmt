<div class="container">

    <%= render :partial => "home/header" %>
		
	<div class="main">

		<div class="item">
            
            <div class="date">
                <div>&nbsp</div>
                <span>&nbsp</span>
            </div>

            <div class="content">
                <h1>Listing Donors</h1>
                
                <div class="body">  
                
                    <% if @donors.nil? || @donors.empty? %>
                        
                        <p>There are no donors.</p>
                    
                    <% else %>
                    
                        <table>
                          <tr>
                            <th>Name</th>
                            <th>Type</th>
                            <th></th>
                            <th></th>
                            <th></th>
                          </tr>

                        <% @donors.each do |donor| %>
                            <tr bgcolor="<%= cycle("E8F6BF", "FFFFFF")%>">
                            <td><%= donor.name %></td>
                            <td><%= donor.dtype.description unless (donor.dtype.nil?) %></td>
                            <td class="actions"><%= link_to 'Show', donor %></td>
                            <td class="actions"><%= link_to 'Edit', edit_donor_path(donor) %></td>
                            <td class="actions"><%= link_to 'Delete', donor, :confirm => 'Are you sure?', :method => :delete %></td>
                          </tr>
                        <% end %>
                        </table>
                        
                    <% end %>

                    <br />

                    <%= link_to 'New Donor', new_donor_path %>

                </div>
            </div>
        </div>
	</div>

    <%= render :partial => "home/navigation" %>
	
	<div class="clearer"><span></span></div>

    <%= render :partial => "home/footer" %>

</div>    