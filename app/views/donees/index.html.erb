<div class="container">

    <%= render :partial => "home/header" %>
		
	<div class="main">

		<div class="item">
            
            <div class="date">
                <div>&nbsp</div>
                <span>&nbsp</span>
            </div>

            <div class="content">
                <h1>Listing Donees</h1>
                
                <div class="body">  
                
                    <% if @donees.nil? || @donees.empty? %>
                        
                        <p>There are no donees.</p>
                    
                    <% else %>
                    
                        <table width="100%">
                          <tr width="100%">
                            <th>Name</th>
                            <th>Location</th>
                            <th>Type</th>
                            <th width="6%"></th>
                            <th width="8%"></th>
                          </tr>

                        <% @donees.each do |donee| %>
                          <tr>
                            <td><%= donee.name %></td>
                            <td><%= donee.location.get_location_name %></td>
                            <td><%= donee.dtype.description unless (donee.dtype.nil?) %></td>
                            <td><%= link_to 'Show', donee %></td>
                            <td><%= link_to 'Edit', edit_donee_path(donee) %></td>
                          </tr>
                        <% end %>
                        </table>
                        
                    <% end %>

                    <br />

                    <%= link_to 'New Donee', new_donee_path %>

                </div>
            </div>
        </div>
	</div>

    <%= render :partial => "home/navigation" %>
	
	<div class="clearer"><span></span></div>

    <%= render :partial => "home/footer" %>

</div>    