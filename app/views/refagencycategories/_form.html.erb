<%= form_for(@refagencycategory) do |f| %>
  <% if @refagencycategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@refagencycategory.errors.count, "error") %> prohibited this refagencycategory from being saved:</h2>

      <ul>
      <% @refagencycategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <% if params[:action] == "new"
            @submit_label = "Create Category"
       else
            @submit_label = "Update Category"
       end %>
    <%= f.submit @submit_label%>
  </div>
<% end %>
